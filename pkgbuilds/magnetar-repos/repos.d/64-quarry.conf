# quarry
#
# Personal repository of an Arch Linux developer, mostly Ruby and Go
# packaging. Signed with their @archlinux.org key.
#
# Measured against core+extra+multilib: 1685 packages, 19 of which share a
# name with something Arch ships. Position in this directory is what keeps
# those 19 from winning — every file here sorts below the Arch repositories
# declared in /etc/pacman.conf, so a collision resolves to Arch.
#
# Signing key, read off a signature this repository actually served:
#   8E1992167465DB5FB045557CB02854ED753E0F1F
#   Anatol Pomozov <anatolik@archlinux.org>
#
# SigLevel is Required, like everything else Magnetar configures. This
# repository IS signed; it is only unusable until its key is imported and
# locally signed, which is what `magnetar-repo enable quarry` does. A
# SigLevel of Never would "work" immediately and would mean running install
# scripts as root from whatever answers the DNS.
#
# Disabled until enabled deliberately:
#
#   magnetar-repo enable quarry
#
# >>> magnetar-repo:quarry
#[quarry]
#SigLevel = Required DatabaseOptional
#Server = https://pkgbuild.com/~anatolik/quarry/$arch
# <<< magnetar-repo:quarry
